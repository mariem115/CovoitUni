security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
                success_handler: App\Security\LoginSuccessHandler
            logout:
                path: app_logout
                target: app_home
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800

    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/connexion, roles: PUBLIC_ACCESS }
        - { path: ^/inscription, roles: PUBLIC_ACCESS }
        - { path: ^/$, roles: PUBLIC_ACCESS }
        - { path: ^/trajets/nouveau, roles: ROLE_CONDUCTEUR }
        - { path: ^/trajets/\d+/modifier, roles: ROLE_CONDUCTEUR }
        - { path: ^/trajets/\d+/supprimer, roles: ROLE_CONDUCTEUR }
        - { path: ^/mes-trajets, roles: ROLE_CONDUCTEUR }
        - { path: ^/reserver, roles: ROLE_PASSAGER }
        - { path: ^/mes-reservations, roles: ROLE_PASSAGER }
        - { path: ^/noter, roles: ROLE_PASSAGER }
        - { path: ^/trajets$, roles: ROLE_PASSAGER }
        - { path: ^/trajet/, roles: ROLE_USER }
        - { path: ^/trajets/\d+, roles: ROLE_USER }
        - { path: ^/reservation/\d+/confirmer, roles: ROLE_CONDUCTEUR }
        - { path: ^/reservation/\d+/refuser, roles: ROLE_CONDUCTEUR }
        - { path: ^/reservation/\d+/annuler, roles: ROLE_PASSAGER }
        - { path: ^/reservation/, roles: ROLE_USER }
        - { path: '^/profil($|/)', roles: ROLE_USER }
        - { path: ^/conducteur/, roles: ROLE_USER }
        - { path: ^/mon-profil, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
