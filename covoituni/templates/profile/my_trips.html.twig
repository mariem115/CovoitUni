{% extends 'base.html.twig' %}

{% block title %}Mes trajets — {{ parent() }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
        <h1 class="h3 fw-bold mb-0">Mes trajets</h1>
        <a href="{{ path('app_trip_new') }}" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg me-1"></i>Nouveau trajet</a>
    </div>

    {% if trips is empty %}
        <div class="card border-0 shadow-sm text-center py-5">
            <p class="text-muted mb-3">Vous n’avez pas encore publié de trajet.</p>
            <a href="{{ path('app_trip_new') }}" class="btn btn-primary">Proposer un trajet</a>
        </div>
    {% else %}
        <div class="row g-4">
            {% for trip in trips %}
                <div class="col-12 col-md-6 col-xl-4">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-body d-flex flex-column">
                            <div class="d-flex justify-content-between align-items-start gap-2 mb-2">
                                <h2 class="h6 fw-semibold mb-0">{{ trip.departure }} → {{ trip.destination }}</h2>
                                {% if trip.isActive %}
                                    <span class="badge bg-success-subtle text-success border">Actif</span>
                                {% else %}
                                    <span class="badge bg-secondary-subtle text-secondary border">Retiré</span>
                                {% endif %}
                            </div>
                            <p class="small text-muted mb-2">{{ trip.departureDateTime|date('d/m/Y H:i') }}</p>
                            <p class="small mb-3">{{ trip.seatsAvailable }} place(s) libre(s) · {{ trip.pricePerSeat ? trip.pricePerSeat|number_format(2, ',', ' ') ~ ' TND' : 'Gratuit' }}</p>
                            <div class="mt-auto d-flex flex-wrap gap-2">
                                <a href="{{ path('app_trip_show', { id: trip.id }) }}" class="btn btn-outline-primary btn-sm">Voir</a>
                                {% if is_granted('EDIT', trip) %}
                                    <a href="{{ path('app_trip_edit', { id: trip.id }) }}" class="btn btn-outline-secondary btn-sm">Modifier</a>
                                {% endif %}
                                {% if is_granted('DELETE', trip) and trip.isActive %}
                                    <form method="post" action="{{ path('app_trip_delete', { id: trip.id }) }}" class="d-inline" onsubmit="return confirm('Retirer ce trajet ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete_trip_' ~ trip.id) }}">
                                        <button type="submit" class="btn btn-outline-danger btn-sm">Supprimer</button>
                                    </form>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}
